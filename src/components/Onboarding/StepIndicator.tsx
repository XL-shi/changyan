// StepIndicator is superseded by the inline step list in OnboardingLayout.
// Kept as a stub to avoid orphaned imports in tests or future use.

interface Props {
  total: number
  current: number
}

export function StepIndicator(_props: Props) {
  return null
}
