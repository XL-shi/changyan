---
description: 未明确要求时不自动写设计/计划类 Markdown，节省上下文与 token
alwaysApply: true
---

# 文档与回复体量

- 除非用户**明确说**要写设计文档、实施计划、`docs/` 下的说明、或要求提交/沉淀到仓库的 Markdown，否则**不要创建或修改** `docs/**/*.md`、`**/plans/**/*.md`、`*-design.md`、`*-plan*.md` 等计划类文件。
- 普通功能或 bug 修复：在对话里用少量要点说明方案，用户确认后直接改代码即可。
- 回复保持简洁；避免长篇流程说明、重复罗列已显而易见的步骤。
- 若用户只要代码改动，默认**不**为「走流程」单独产出文档。
